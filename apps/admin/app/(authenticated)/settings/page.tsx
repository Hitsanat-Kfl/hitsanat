import SettingsPage from "@/features/settings/components/settings-page";

export const metadata = {
  title: "Settings | Hitsanat Kifl",
};

export default function SettingsRoute() {
  return <SettingsPage />;
}
